differential_privacy:
  enabled: true
  epsilon_values: [0.5, 1.0, 2.0, 5.0]
  delta: 1.0e-5
  max_grad_norm: 1.0
  noise_multiplier: null
  mechanism: "gaussian"
  accountant: "rdp"

federation:
  num_rounds: 50
  local_epochs: 3
  strategy: "fedavg"
